import 'package:flutter/widgets.dart';

class RootIcon extends StatelessWidget {
  const RootIcon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('/ ');
  }
}
